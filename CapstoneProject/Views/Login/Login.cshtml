@model CapstoneProject.Models.LoginViewModel

<<<<<<< HEAD
@section Styles {
    <link rel="stylesheet" href="~/css/login.css" asp-append-version="true" />
}


<!DOCTYPE html>
<html>
<head>
    <title>Login</title>
    <link rel="stylesheet" href="~/css/login.css" />
</head>
<body>
    <div class="login-container">
        <h2>Login</h2>
        @using (Html.BeginForm("Login", "Login", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div>
                @Html.TextBoxFor(m => m.Username, new { placeholder = "Username" })
                @Html.ValidationMessageFor(m => m.Username)
            </div>
            <div>
                @Html.PasswordFor(m => m.Password, new { placeholder = "Password" })
                @Html.ValidationMessageFor(m => m.Password)
            </div>
            <button type="submit">Login</button>
        }

        <!-- Language Selection -->
        <div class="language-container">
            <img src="https://upload.wikimedia.org/wikipedia/commons/a/a4/Flag_of_the_United_States.svg" alt="English" />
            <img src="https://upload.wikimedia.org/wikipedia/commons/c/c3/Flag_of_France.svg" alt="French" />
            <img src="https://upload.wikimedia.org/wikipedia/commons/9/9a/Flag_of_Spain.svg" alt="Spanish" />
        </div>

        <!-- Footer -->
        <div class="footer-container">
            <p>&copy; 2025 SSP. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
=======
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - SSP</title>
    <link rel="stylesheet" href="~/css/login.css" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
</head>
<body class="d-flex flex-column justify-content-center align-items-center" style="height: 100vh; background-color: #f8f9fa;">

    <div class="logo-container">
        <img src="~/images/logo.png" alt="SSP Logo" class="logo">
    </div>

    <div class="login-form text-center p-4">
        @using (Html.BeginForm("Login", "Login", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div class="form-container">
                <div class="mb-3">
                    @Html.TextBoxFor(m => m.Username, new { @class = "form-control", placeholder = "Username" })
                    @Html.ValidationMessageFor(m => m.Username, "", new { @class = "text-danger" })
                </div>
                <div class="mb-3">
                    @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "Password" })
                    @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-check form-switch mt-3">
                <input class="form-check-input" type="checkbox" id="rememberMe">
                <label class="form-check-label" for="rememberMe">Remember Password?</label>
            </div>

            <button type="submit" class="btn btn-primary w-100 mt-4 login-btn">Log In</button>
        }
    </div>

</body>
</html>

<style>
    
    .logo-container {
        text-align: center;
        margin-bottom: 30px;
        margin-top: -50px; 
    }

    .logo {
        width: 350px; 
        height: auto;
    }

    .login-form {
        text-align: center;
        max-width: 450px;
    }

    .form-container {
        padding: 20px;
        border-radius: 50px;
        background: rgba(200, 200, 200, 0.2);
    }

    .form-control {
        border: none;
        border-radius: 30px;
        padding: 12px;
        text-align: center;
        width: 100%;
        box-shadow: inset 2px 2px 5px rgba(0, 0, 0, 0.1);
    }

    .btn-primary.login-btn {
        border-radius: 30px;
        padding: 12px;
        font-size: 1.1rem;
        width: 100%;
        background: #6b4ca3;
        border: none;
        transition: background 0.3s ease-in-out;
    }

        .btn-primary.login-btn:active {
            background: #007bff;
        }

</style>
>>>>>>> cb8ead6 (Updated files with layout fixes and new style in settings)
